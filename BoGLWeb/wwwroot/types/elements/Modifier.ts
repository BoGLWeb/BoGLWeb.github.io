// provides an ID and name for a modifier
export class Modifier {
    id: number;
    name: string;

    constructor(id: number, name: string) {
        this.id = id;
        this.name = name;
    }
}