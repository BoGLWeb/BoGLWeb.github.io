class BondGraphElement {
    id: number;
    img: string;
    x: number;
    y: number;

    constructor(id: number, img: string, x: number, y: number) {
        this.id = id;
        this.img = img;
        this.x = x;
        this.y = y;
    }
}